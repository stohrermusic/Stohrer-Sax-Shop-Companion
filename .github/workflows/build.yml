name: Build Releases

on:
  push:
    branches: [main, beta, gamma]
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: StohrerSaxShopCompanion.exe
            asset_name: StohrerSaxShopCompanion-Windows.exe
          - os: macos-13
            artifact_name: StohrerSaxShopCompanion.app
            asset_name: StohrerSaxShopCompanion-macOS-Intel.zip
          - os: macos-latest
            artifact_name: StohrerSaxShopCompanion.app
            asset_name: StohrerSaxShopCompanion-macOS-AppleSilicon.zip
          - os: ubuntu-latest
            artifact_name: StohrerSaxShopCompanion
            asset_name: StohrerSaxShopCompanion-Linux

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Set macOS deployment target
        if: runner.os == 'macOS'
        run: echo "MACOSX_DEPLOYMENT_TARGET=11.0" >> $GITHUB_ENV

      - name: Build with PyInstaller
        run: python build.py

      # Rename artifacts for release
      - name: Rename Windows artifact
        if: matrix.os == 'windows-latest'
        run: mv dist/StohrerSaxShopCompanion.exe dist/StohrerSaxShopCompanion-Windows.exe

      - name: Rename Linux artifact
        if: matrix.os == 'ubuntu-latest'
        run: mv dist/StohrerSaxShopCompanion dist/StohrerSaxShopCompanion-Linux

      # macOS: Create zip of .app bundle
      - name: Package macOS app
        if: runner.os == 'macOS'
        run: |
          cd dist
          zip -r ${{ matrix.asset_name }} StohrerSaxShopCompanion.app

      # Upload artifacts for all builds
      - name: Upload Windows artifact
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: StohrerSaxShopCompanion-Windows
          path: dist/StohrerSaxShopCompanion-Windows.exe

      - name: Upload macOS Intel artifact
        if: matrix.os == 'macos-13'
        uses: actions/upload-artifact@v4
        with:
          name: StohrerSaxShopCompanion-macOS-Intel
          path: dist/StohrerSaxShopCompanion-macOS-Intel.zip

      - name: Upload macOS Apple Silicon artifact
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: StohrerSaxShopCompanion-macOS-AppleSilicon
          path: dist/StohrerSaxShopCompanion-macOS-AppleSilicon.zip

      - name: Upload Linux artifact
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: StohrerSaxShopCompanion-Linux
          path: dist/StohrerSaxShopCompanion-Linux

      # Attach to release if this is a release event
      - name: Upload to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/${{ matrix.asset_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
